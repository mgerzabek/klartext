---
#
---

function createCookie(name, value, days) {
  var date = new Date();
  date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
  expires = "; expires=" + date.toUTCString();
  document.cookie = name + "=" + value + expires + "; path=/";
}
function readCookie(name) {
  var nameEQ = name + "=";
  var ca = document.cookie.split(';');
  for (var i = 0; i < ca.length; i++) {
    var c = ca[i];
    while (c.charAt(0) == ' ') c = c.substring(1, c.length);
    if (c.indexOf(nameEQ) == 0) return c.substring(nameEQ.length, c.length);
  }
  return null;
}
function eraseCookie(name) {
  createCookie(name, "", -1);
}

if (readCookie('accept-cookies') == 'true') {
  {% if jekyll.environment == 'production' and site.gtm %}
  window['ga-disable-{{ site.gtm }}'] = window.doNotTrack === "1" || navigator.doNotTrack === "1" || navigator.doNotTrack === "yes" || navigator.msDoNotTrack === "1";
  window.dataLayer = window.dataLayer || [];
  function gtag() { dataLayer.push(arguments); }
  gtag('js', new Date());
  gtag('config', '{{ site.gtm }}');
  {% endif %}
  document.getElementById('cookie-notice').style.display = 'none';
} else if (readCookie('accept-cookies') == 'false') {
  document.getElementById('cookie-notice').style.display = 'none';
}
else {
  document.getElementById('cookie-notice').style.display = 'block';
  document.getElementById('accept-cookies').addEventListener("click", function () {
    createCookie('accept-cookies', 'true', 31);
    document.getElementById('cookie-notice').style.display = 'none';
    location.reload();
  });
  document.getElementById('deny-cookies').addEventListener("click", function () {
    createCookie('accept-cookies', 'false', 1);
    document.getElementById('cookie-notice').style.display = 'none';
    location.reload();
  });
}